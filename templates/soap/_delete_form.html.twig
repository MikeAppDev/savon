<form method="post" action="{{ path('app_soap_delete', {'id': soap.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soap.id) }}">
    <button class="btn">Delete</button>
</form>
