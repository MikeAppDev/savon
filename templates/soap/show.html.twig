{% extends 'base.html.twig' %}

{% block title %}Soap{% endblock %}

{% block body %}
    <h1>Soap</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soap.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ soap.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ soap.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ soap.price }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ soap.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_soap_index') }}">back to list</a>

    <a href="{{ path('app_soap_edit', {'id': soap.id}) }}">edit</a>

    {{ include('soap/_delete_form.html.twig') }}
{% endblock %}
